# Place your test video here as test-clip.mp4
# The file is gitignored (large media). To create one:
#   ffmpeg -i <your-video.mp4> -vf scale=1280:720 -c:v libx264 -preset fast -crf 23 -pix_fmt yuv420p -an test-clip.mp4
